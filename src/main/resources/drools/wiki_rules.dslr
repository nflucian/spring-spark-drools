package drools

import ro.neghina.sparkapp.entities.LangRating

expander wiki.dsl

rule "Lang good Score"
    when
        If there is a Language with score greater than 500
    then
        Say "Language approved"
    end